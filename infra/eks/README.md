# infra/eks — Phase 6 (stub)

EKS cluster + node group + manifests (Deployment/Service/Ingress/HPA).
See `docs/phases/phase-6-eks.md`.

**Most expensive phase: EKS control plane ~$0.10/hr even idle. Apply and `terraform
destroy` the SAME DAY.**
